# 退课退款演示 PRD 说明

## 1. 项目背景

本项目是一个用于演示“退课”和“特殊退费”流程的前端原型页，当前实现为单页应用，所有订单、课次、优惠和退款金额均为本地硬编码数据，没有后端接口、没有真实提交流程、也没有数据持久化。

这个页面的核心目的不是做成一个完整业务系统，而是把教务/运营/财务在退款场景下会遇到的规则、计算方式和课次级明细展示出来，便于产品、开发、测试在同一页面上验证逻辑。

---

## 2. 产品目标

### 2.1 核心目标

1. 展示一个课程订单的基础信息。
2. 支持两类业务入口：
   - 退课演示
   - 财务特殊退费演示
3. 在抽屉中模拟不同退款场景下的课次选择、金额计算、优惠说明和明细表展示。
4. 支持查看金额计算详情，帮助业务方理解“为什么是这个退款金额”。

### 2.2 非目标

1. 不做真实接口调用。
2. 不做订单状态落库。
3. 不做退款提交后的流程流转。
4. 不做权限控制、审批流、日志系统。

---

## 3. 页面结构

页面由三个主要区域组成：

1. 主入口区
   - 退课演示
   - 财务特殊退费演示
2. 右侧抽屉
   - 用于处理退课/退款申请
3. 明细弹窗
   - 用于查看优惠说明、实付金额明细、本次退款金额明细

---

## 4. 首页入口设计

### 4.1 退课演示入口

首页左侧区域展示 6 个退课场景按钮，按角色分组：

- 运营视角
  - 无任何优惠案例
  - 有优惠-按优惠价计费案例
  - 有优惠-按原价计费案例
- 财务视角
  - 无任何优惠案例
  - 有优惠-按优惠价计费案例
  - 有优惠-按原价计费案例

点击任一按钮后：

1. 打开右侧抽屉。
2. 自动切换到“退课”模式。
3. 根据场景设置付款方式、优惠类型、课次选择模式。

### 4.2 财务特殊退费入口

首页右侧区域展示 3 个入口：

- 原订单无任何优惠案例
- 原订单存在优惠案例
- 批量导入特殊退费

其中：

- 前两个入口可点击并打开抽屉。
- “批量导入特殊退费”目前为禁用态，仅展示，不可点击。

点击前两个入口后：

1. 打开右侧抽屉。
2. 自动切换到“特殊退费”模式。
3. 根据选择的场景初始化特殊退费规则。

---

## 5. 业务对象说明

### 5.1 课次数据

系统内置 15 节课，课次基础状态如下：

- 第 1-5 节：已下课
- 第 6-15 节：可选课次

课次配送方式如下：

- 第 1-2 节：线上课
- 第 3-10 节：线下课
- 第 11-15 节：线上课

### 5.2 课次状态

当前代码中课次状态存在以下枚举：

- `completed`：已下课
- `selectable`：可选择
- `refunded`：已退款

注意：

- 当前原型中大部分课次状态是静态模拟，只有界面展示层会根据选中状态、退款场景动态改变标签。
- 真正的“已退款”状态并不是通过接口更新，而是写死在模拟逻辑里或仅通过展示推导。

### 5.3 金额基准

基础金额规则：

- 课程原价：`¥210.00 / 课次`
- 无优惠订单实付总额：`¥3150.00`
- 有优惠订单实付总额：`¥1575.00`
- 有优惠时单课次实付金额默认按 `¥105.00` 计算

特殊规则中还使用：

- 线上课返利金额：`¥15.00 / 节`
- 高端班半价退费：`¥105.00 / 节`

---

## 6. 抽屉模式总览

抽屉顶部有两个 Tab：

- 退课
- 特殊退费

两个模式的差异如下：

| 模式 | 主要用途 | 课次选择方式 | 金额计算方式 |
|---|---|---|---|
| 退课 | 模拟退课申请 | 单选起始课次或多选课次 | 按场景不同计算退课金额 |
| 特殊退费 | 模拟特殊业务退费 | 按场景支持多选或输入金额 | 按业务规则计算返利/差价/分摊 |

---

## 7. 退课模式 PRD

### 7.1 场景枚举

退课模式支持 3 种场景：

1. `standard`
   - 无任何优惠
2. `discount_activity`
   - 有优惠，按优惠价计费
3. `discount_original`
   - 有优惠，但按原价计费

### 7.2 退课模式初始化规则

点击首页退课入口后：

1. `mode = withdraw`
2. 根据入口设置 `scenario`
3. 若为无优惠场景，默认退款方式为“现金退款”
4. 若为有优惠场景，默认退款方式为“原路退回”
5. 退课原因重置为空
6. 课次选择重置为空
7. 详情弹窗关闭
8. 其他特殊退费状态重置

### 7.3 退课原因

在退课模式中显示“退款原因”下拉框，选项包括：

- 时间冲突
- 距离冲突
- 教师问题
- 课程问题
- 退费重报
- 业务办理错误
- 其他

说明：

- 该字段目前仅为界面输入项，没有接入提交校验或后端保存。

### 7.4 退款说明

提供一个多行文本框用于填写退款说明。

说明：

- 当前仅是输入框，未绑定到提交逻辑。

### 7.5 课次选择规则

退课模式下有两种选择方式：

#### 7.5.1 range 模式

适用于运营视角。

- 用户点击一个起始课次。
- 系统自动把该课次及之后的所有课次视为待退范围。
- 仅允许选择第 6-15 节。
- 已下课课次和已退款课次不可再次选择。

#### 7.5.2 multi 模式

适用于财务视角。

- 用户可以多选课次。
- 未退款课次均可点击选择。
- 已退款课次不可选。
- 已下课课次仍可被选中，用于财务场景下的特殊处理。

### 7.6 退课金额规则

#### 7.6.1 无优惠场景

当 `scenario = standard` 时：

- 每选择 1 节课，退款金额 = `¥210.00`

即：

`退款金额 = 选中课次数 × 210`

#### 7.6.2 有优惠场景，按优惠价计费

当 `scenario = discount_activity` 时：

- 每选择 1 节课，退款金额 = `¥105.00`

即：

`退款金额 = 选中课次数 × 105`

#### 7.6.3 有优惠场景，按原价计费

当 `scenario = discount_original` 时：

- 前 7 节课按原价 `¥210.00` 计算
- 第 8 节课按剩余实付金额计算
- 第 9-15 节课按 `¥0.00` 计算

当前实现里总实付金额仍按 `¥1575.00` 计算，因此：

- 前 7 节课退款价值：`7 × 210 = 1470`
- 第 8 节课退款价值：`1575 - 1470 = 105`
- 第 9-15 节课退款价值：`0`

这个规则用于模拟“优惠订单按原价顺序消耗实付金额”的退费逻辑。

### 7.7 退课金额明细弹窗

当用户在退课模式下点击金额旁边的问号图标时，打开“本次退款金额明细”弹窗。

弹窗内展示：

- 优惠说明
- 课次明细表
- 退款合计

#### 明细表字段

- 课次
- 课次状态
- 原价
- 优惠金额
- 课耗金额
- 已退金额
- 剩余可退金额

#### 已退金额说明

该字段的 tooltip 统一说明：

`可退金额 = 退课已退金额 + 线上课已返利金额 + 高端班半价已退金额 + 其他特殊退费已退金额`

### 7.8 退课确认

底部确认按钮文案：

- 退课模式：`确认退课`

点击后：

- 当前仅显示一个“申请已提交”的 toast 提示
- 不会真正提交到后端
- 不会关闭抽屉
- 不会改变服务端状态

按钮禁用规则：

- 当本次退款金额小于等于 0 时禁用

---

## 8. 特殊退费模式 PRD

### 8.1 场景枚举

特殊退费模式支持 4 种场景：

1. `online_rebate`
   - 线上课返利
2. `high_end_half`
   - 高端班半价
3. `discount_diff`
   - 优惠活动退差价
4. `single_lesson`
   - 单课次退款
5. `custom_refund`
   - 自定义退费

### 8.2 场景切换规则

切换场景时：

1. 关闭优惠下拉菜单
2. 清空已选课次
3. 清空自定义退款金额
4. 自定义分摊方式回到默认值 `spread`
5. 若切到 `discount_diff`，默认优惠方案重置为 `special_five`
6. 若切到 `custom_refund`，默认分摊方式显示为“分摊到每个课次”

### 8.3 场景说明

#### 8.3.1 线上课返利

规则：

- 已下课的线上课才能返利
- 返利金额固定为 `¥15.00 / 节`
- 仅第一节符合条件的课次显示实际返利，其余符合条件课次显示剩余可退状态

界面表现：

- 展示“剩余可返利金额”
- 已返利课次显示为不可再选状态

#### 8.3.2 高端班半价

规则：

- 高端班课次每节退回一半课耗金额，即 `¥105.00 / 节`
- 当前代码里高端班从第 4 节起做标签展示，但退款可退范围主要从第 5 节起开始显示为可退

界面表现：

- 展示“高端班剩余可退金额”
- 非高端班、未下课、已返利课次会被置灰或禁用

#### 8.3.3 优惠活动退差价

规则：

- 支持切换优惠方案
- 系统会对比“原优惠金额”和“现优惠金额”
- 退回差价 = 当前优惠金额 - 原优惠金额

当前默认对比方式：

- 原优惠：固定视为 5 折
- 当前优惠：可在下拉列表中选择不同折扣

明细表展示字段：

- 原价
- 原优惠金额
- 现优惠金额
- 优惠可退差价
- 更换优惠后课耗金额

#### 8.3.4 单课次退款

规则：

- 支持多选课次
- 退款金额 = 所选课次的实付金额合计

如果订单有优惠：

- 单课次实付金额默认按 `¥105.00`

如果订单无优惠：

- 单课次实付金额按 `¥210.00`

界面表现：

- 需要先选择课次
- 可展开查看未选课次

#### 8.3.5 自定义退费

规则：

- 用户手动输入退款金额
- 系统根据可退上限进行封顶
- 支持两种分摊方式：
  - 分摊到每个课次
  - 指定课次

##### 分摊到每个课次

- 输入金额会均摊到全部课次
- 可退总额封顶为当前场景允许的最大金额

##### 指定课次

- 用户先多选课次
- 输入金额会按所选课次的支付能力进行分摊
- 若输入金额超过所选课次可承受金额，显示提示：
  - 所选课次可分摊金额不足，请增加课次或减少退款金额。

### 8.4 特殊优惠方案

特殊退费中的优惠方案列表包括：

- 比心计划8折
- 比心计划7折
- 比心计划6折
- 比心计划5折
- 比心计划4折
- 比心计划3折
- 比心计划2折
- 比心计划1折
- 内部员工子女2折（包含亲兄弟姐妹）
- 特殊关系9折
- 特殊关系8折
- 特殊关系5折（按优惠价）
- 特殊关系5折（按原价）

其中：

- “按优惠价计费”表示优惠金额平均分摊到所有课次
- “按原价计费”表示先按原价顺序消耗实付金额，实付耗尽后后续课次金额为 0

### 8.5 特殊退费确认

底部确认按钮文案：

- 特殊退费模式：`确认退款`

按钮禁用规则：

- 退款金额小于等于 0 时禁用

点击后：

- 仅展示“退款申请已提交” toast
- 不执行真实退款

---

## 9. 明细弹窗说明

页面支持三类明细弹窗：

1. 优惠说明
2. 实付金额明细
3. 本次退款金额明细

### 9.1 优惠说明

从订单信息区点击优惠金额旁的问号打开。

展示内容：

- 当前优惠名称
- 当前优惠规则

### 9.2 实付金额明细

从订单信息区点击实付金额旁的问号打开。

展示内容：

- 当前优惠名称
- 当前优惠规则
- 课次明细表

### 9.3 本次退款金额明细

从金额汇总区的问号打开。

展示内容：

- 当前退款的计算方式
- 课次明细表
- 退款合计

### 9.4 明细弹窗通用行为

- 右上角关闭按钮可关闭弹窗
- 点击遮罩层可关闭弹窗
- 弹窗关闭后返回原抽屉状态

---

## 10. 当前数据模型与状态

### 10.1 页面状态

当前页面使用本地状态管理，核心状态包括：

- `drawerOpen`
- `detailView`
- `showAllLessonRows`
- `mode`
- `scenario`
- `specialScenario`
- `specialDiscount`
- `discountDropdownOpen`
- `specialSelectedLessonIds`
- `customRefundAmount`
- `customAllocationMode`
- `withdrawSelectionMode`
- `withdrawSelectedLessonIds`
- `refundMethod`
- `reason`
- `selectedStart`
- `confirmed`

### 10.2 关键衍生状态

系统还通过计算生成以下衍生值：

- 是否有优惠
- 是否为按原价退款
- 当前付款方式
- 选中课次集合
- 退款金额
- 特殊退费可退总额
- 是否需要更多课次参与分摊
- 是否可以确认提交

### 10.3 当前实现特点

1. 所有数据都在单文件组件中计算。
2. 没有拆分成独立业务 hook。
3. 没有 API 层。
4. 金额逻辑和 UI 逻辑紧密耦合，适合原型验证，不适合直接作为生产架构。

---

## 11. 计算规则汇总

### 11.1 通用金额基准

| 项目 | 金额 |
|---|---:|
| 原价 | ¥210.00 |
| 无优惠总实付 | ¥3150.00 |
| 有优惠总实付 | ¥1575.00 |
| 有优惠单课次实付 | ¥105.00 |
| 线上返利 | ¥15.00 |
| 高端班半价 | ¥105.00 |

### 11.2 退课模式金额公式

#### 无优惠

`退款金额 = 选中课次数 × 210`

#### 有优惠-按优惠价

`退款金额 = 选中课次数 × 105`

#### 有优惠-按原价

- 第 1-7 节：`210`
- 第 8 节：`1575 - 7×210 = 105`
- 第 9-15 节：`0`

### 11.3 特殊退费金额公式

#### 线上课返利

`返利金额 = 已下课的线上课中，符合返利条件的课次 × 15`

#### 高端班半价

`退费金额 = 符合高端班条件的已下课课次 × 105`

#### 优惠活动退差价

`可退差价 = 现优惠金额 - 原优惠金额`

#### 单课次退款

`退款金额 = 所选课次的实付金额合计`

#### 自定义退费

`退款金额 = min(输入金额, 最大可退款金额)`

---

## 12. 交互边界与异常处理

### 12.1 选择限制

1. 退课 range 模式下，不能选择第 1-5 节。
2. 退课 mode 下已退款课次不可再次选择。
3. 特殊退费里不同场景有各自的可选限制。

### 12.2 退款方式限制

当付款方式是“现金支付”时：

- 退款方式下拉框置灰
- 只能选择现金退款
- 鼠标悬浮会提示“现金支付订单仅支持现金退款”

### 12.3 退款金额校验

1. 退款金额为 0 时，确认按钮禁用。
2. 自定义退费超出上限时自动封顶。
3. 指定课次分摊金额不足时显示提示。

### 12.4 关闭重置规则

点击关闭抽屉时会重置：

- 明细弹窗
- 展开全部行状态
- 确认成功提示
- 特殊退费课次选择
- 自定义金额
- 分摊方式
- 优惠下拉状态
- 退课选择模式
- 退课已选课次

这意味着抽屉关闭后再次打开，会回到初始化状态。

---

## 13. 当前实现中的开发注意点

1. 这是一个强演示性质的页面，UI 和计算逻辑都写在 `src/app/App.tsx` 中。
2. 金额计算依赖大量条件分支，后续若要接真实业务，建议拆成：
   - 课次规则计算层
   - 金额计算层
   - 场景配置层
   - UI 展示层
3. 当前页面存在“展示逻辑即业务逻辑”的写法，适合快速演示，但不适合长期维护。
4. “批量导入特殊退费”目前只是占位入口。
5. 确认按钮仅模拟提交，后续接真实接口时需要补：
   - 表单校验
   - 提交 loading
   - 成功/失败状态
   - 关闭与刷新逻辑

---

## 14. 建议的后续开发拆分

如果后续要继续迭代，建议按下面顺序拆分：

1. 抽出订单数据模型和课次规则常量。
2. 抽出退课金额计算函数。
3. 抽出特殊退费场景配置表。
4. 抽出可复用的课次表格组件。
5. 接入真实接口和提交状态。
6. 加入表单校验与错误提示。

---

## 15. 一句话总结

当前页面是一个用于展示“退课”和“特殊退费”规则的前端原型，重点在于场景切换、课次选择、金额计算和明细解释，所有数据均为本地模拟，便于开发和业务快速对齐逻辑。

