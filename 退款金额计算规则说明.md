# 退款金额计算规则说明

> 说明：本文只整理“退款金额怎么算”，不展开页面功能、UI 结构和交互流程。  
> 以下规则均基于当前前端实现 `src/app/App.tsx` 的实际计算逻辑。

## 1. 基础参数

### 1.1 课次基础值

- 课程原价：`210`
- 默认单课次实付金额：
  - 无优惠：`210`
  - 有优惠：`105`
- 订单总价：`3150`
- 有优惠订单总实付：`1575`
- 线上课返利金额：`15`
- 高端班半价退费金额：`105`

### 1.2 课次数据

当前页面固定模拟 15 节课：

- 第 1-5 节：已下课
- 第 6-15 节：可选课次
- 第 1-2 节：线上课
- 第 3-10 节：线下课
- 第 11-15 节：线上课

---

## 2. 退课模式的退款计算

退课模式下，最终退款金额由 `refundAmount` 计算。

### 2.1 无优惠订单

适用条件：

- `scenario = standard`
- 订单无优惠

计算方式：

```text
退款金额 = 选中课次数 × 210
```

举例：

- 选中 1 节，退款 `210`
- 选中 3 节，退款 `630`

---

### 2.2 有优惠订单，按优惠价计费

适用条件：

- `scenario = discount_activity`
- 订单存在优惠，但退费按优惠后的实付金额计算

计算方式：

```text
退款金额 = 选中课次数 × 105
```

举例：

- 选中 1 节，退款 `105`
- 选中 4 节，退款 `420`

---

### 2.3 有优惠订单，按原价计费

适用条件：

- `scenario = discount_original`
- 订单存在优惠，但退费按“原价顺序消耗实付金额”的方式计算

当前代码的计算规则是：

- 第 1-7 节课：每节按 `210` 计算
- 第 8 节课：按剩余实付金额 `105` 计算
- 第 9-15 节课：按 `0` 计算

可直接理解为：

```text
退款金额 = 选中课次对应的“原价退款额度”之和
```

其中每节课的退款额度为：

- `lessonId <= 7` -> `210`
- `lessonId == 8` -> `105`
- `lessonId >= 9` -> `0`

举例：

- 选中第 1 节 + 第 2 节 = `210 + 210 = 420`
- 选中第 7 节 + 第 8 节 = `210 + 105 = 315`
- 选中第 9 节 + 第 10 节 = `0`

> 备注：这里并不是按“选了几节课 × 固定单价”来算，而是按课次编号对应的剩余可退金额来算。

---

## 3. 特殊退费模式的退款计算

特殊退费模式最终退款金额由 `specialRefundAmount` 计算。

### 3.1 线上课返利

适用条件：

- `specialScenario = online_rebate`

规则：

- 只有“线上课 + 已下课”的课次有返利资格
- 单节返利金额固定为 `15`
- 当前代码中，`getSpecialLessonRefundedAmount` 只给“第一个符合条件的线上已下课课次”标记为已返利
- 其余符合条件但未被标记为已返利的线上已下课课次，仍然保留 `15` 的剩余可退金额

最终退款金额计算：

```text
退款金额 = 所有线上已下课课次的“剩余可返利金额”之和
```

等价理解：

- 已处理掉的返利不重复算
- 剩余未返利的线上已下课课次，每节记 `15`

举例：

- 若有 4 节线上已下课课次，其中 1 节已经视作“已返利”，则本次退款金额 = `3 × 15 = 45`

> 说明：这是当前前端代码的实际行为，后续如果要接业务口径，建议明确“已返利”和“本次可返利”的状态来源。

---

### 3.2 高端班半价

适用条件：

- `specialScenario = high_end_half`

规则：

- 只有已下课课次参与退费
- 每个符合条件的课次按 `210 / 2 = 105` 计算

最终退款金额计算：

```text
退款金额 = 已下课课次数量 × 105
```

举例：

- 5 节已下课课次，退款 `5 × 105 = 525`

> 说明：代码里单行展示会把部分课次标成“高端班”或禁用态，但总金额本质上就是“符合条件的已下课课次 × 105”。

---

### 3.3 优惠活动退差价

适用条件：

- `specialScenario = discount_diff`
- 用户切换了新的优惠方案

当前规则分两套优惠：

- 原优惠：固定按 `50%` 计算
- 现优惠：按用户选中的方案计算

每节课的金额定义：

```text
原优惠金额 = 210 × 0.5
现优惠金额 = 210 × 当前优惠折扣率
可退差价 = 现优惠金额 - 原优惠金额
```

当前退款金额：

```text
退款金额 = 所有课次的可退差价之和
```

其中，当前优惠折扣率由下拉框决定：

- 比心计划8折 -> `0.2`
- 比心计划7折 -> `0.3`
- 比心计划6折 -> `0.4`
- 比心计划5折 -> `0.5`
- 比心计划4折 -> `0.6`
- 比心计划3折 -> `0.7`
- 比心计划2折 -> `0.8`
- 比心计划1折 -> `0.9`
- 特殊关系9折 -> `0.1`
- 特殊关系8折 -> `0.2`
- 特殊关系5折（按优惠价）-> `0.5`
- 特殊关系5折（按原价）-> `0.5`
- 内部员工子女2折（包含亲兄弟姐妹）-> `0.8`

举例：

如果当前优惠为 `5折`：

- 原优惠金额 = `210 × 0.5 = 105`
- 现优惠金额 = `210 × 0.5 = 105`
- 差价 = `0`

如果当前优惠为 `8折`：

- 原优惠金额 = `105`
- 现优惠金额 = `210 × 0.2 = 42`
- 差价 = `42 - 105 = -63`

> 说明：这里的“差价”是按代码直接相减得到的结果，正负号都可能出现，开发时要注意它并不一定是“只会退正数”。

---

### 3.4 单课次退款

适用条件：

- `specialScenario = single_lesson`

规则：

- 用户多选课次
- 每节课按当前课次实付金额退款

计算方式：

```text
退款金额 = 所选课次实付金额之和
```

当前课次实付金额由订单优惠决定：

- 有优惠订单：单节实付 `105`
- 无优惠订单：单节实付 `210`

举例：

- 选中 2 节有优惠课次，退款 `2 × 105 = 210`
- 选中 3 节无优惠课次，退款 `3 × 210 = 630`

---

### 3.5 自定义退费

适用条件：

- `specialScenario = custom_refund`

规则：

- 用户手动输入退款金额
- 系统会先计算一个“最大可退款金额”
- 实际退款金额不会超过最大可退款金额

#### 3.5.1 最大可退款金额

如果分摊方式为“分摊到每个课次”：

```text
最大可退款金额 = 所有课次的可退上限之和
```

如果分摊方式为“指定课次”：

```text
最大可退款金额 = 已选择课次的支付能力之和
```

#### 3.5.2 最终退款金额

```text
退款金额 = min(输入金额, 最大可退款金额)
```

#### 3.5.3 分摊到每个课次

当前代码实现：

```text
每节课分摊金额 = min(输入金额, 最大可退款金额) / 15
```

也就是按全部 15 节课平均分摊。

#### 3.5.4 指定课次

当前代码实现：

```text
所选课次分摊金额 = 输入金额 × 当前课次支付能力 / 所选课次支付能力总和
```

如果输入金额大于所选课次支付能力总和，则会提示：

```text
所选课次可分摊金额不足，请增加课次或减少退款金额。
```

举例：

- 输入 `300`
- 选择 3 节可退课次，每节支付能力 `105`
- 支付能力总和 = `315`
- 每节分摊并非平均，而是按各课次支付能力占比分摊

---

## 4. 通用的课次级计算口径

在明细表里，经常会出现下面几个金额字段。

### 4.1 课耗金额

#### 退课模式

- 已下课课次：
  - 无优惠：`210`
  - 有优惠：`105`
  - 原价退款场景：`210`
- 未下课课次：`0`

#### 特殊退费模式

- 线上课返利：线上已下课课次 `15`
- 高端班半价：已下课课次 `105`
- 优惠活动退差价：按当前优惠下的课耗金额
- 其他场景：按当前课次实付金额

### 4.2 已退金额

已退金额不是统一的固定值，而是按场景取值：

- 退课场景：对应本次选择课次的退款结果
- 线上课返利：当前已返利课次记 `15`
- 高端班半价：当前已退课次记 `105`
- 其他场景：按场景逻辑计算

### 4.3 剩余可退金额

通用公式可以理解为：

```text
剩余可退金额 = 实付金额 - 课耗金额 - 已退金额
```

但不同场景下，实付金额、课耗金额、已退金额的取法不同，所以表格展示要结合场景一起看。

---

## 5. 退款金额的最简速查表

### 5.1 退课模式

| 场景 | 公式 |
|---|---|
| 无优惠 | 选中课次 × 210 |
| 有优惠，按优惠价 | 选中课次 × 105 |
| 有优惠，按原价 | 第 1-7 节 210，第 8 节 105，第 9-15 节 0 |

### 5.2 特殊退费模式

| 场景 | 公式 |
|---|---|
| 线上课返利 | 符合条件的线上已下课课次 × 15 |
| 高端班半价 | 符合条件的已下课课次 × 105 |
| 优惠活动退差价 | Σ(现优惠金额 - 原优惠金额) |
| 单课次退款 | 所选课次实付金额之和 |
| 自定义退费 | min(输入金额, 最大可退款金额) |

---

## 6. 开发时最容易误解的点

1. `discount_diff` 不是“固定退一个正差价”，而是直接做 `现优惠金额 - 原优惠金额`。
2. `discount_original` 不是简单按 `210 × 课次数`，而是有“第 8 节只退剩余实付金额”的阶梯逻辑。
3. `custom_refund` 里“分摊到每个课次”和“指定课次”是两种完全不同的分摊方式。
4. `online_rebate` 的代码里同时存在“已返利”和“剩余可返利”两个概念，最终金额取的是剩余可返利。
5. 页面上的表格展示字段很多，但真正决定最终退款总额的，是对应场景的 `refundAmount` 或 `specialRefundAmount` 计算逻辑。

---

## 7. 建议给开发的实现口径

如果后续要接成真实业务，建议把退款计算拆成下面 4 层：

1. 订单实付层
2. 课次消耗层
3. 已退金额层
4. 本次退款层

这样可以避免把“展示字段”和“结算字段”混在一起，尤其是特殊退费场景会更容易维护。

